PyJWT==2.13.0
annotated-types==0.8.0
anyio==4.14.2
azure-core==1.41.0
azure-identity==1.25.3
azure-search-documents==12.0.0
azure-storage-blob==12.30.0
bcrypt==5.0.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
colorama==0.4.6
cryptography==50.0.0
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
isodate==0.7.2
jiter==0.16.0
msal-extensions==1.3.1
msal==1.37.0
openai==2.53.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pypdf==6.1.1
python-dotenv==1.2.2
requests==2.34.2
resend==2.37.0
sniffio==1.3.1
tqdm==4.70.0
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
azure-identity
azure-keyvault-secrets

# Catches undefined names, which compileall and a successful import both miss.
# tests/test_no_undefined_names.py fails rather than skips without it.
pyflakes==3.2.0

# Document Intelligence: OCR, reading order, and heading roles. Extraction falls back to
# pypdf when DOCUMENT_INTELLIGENCE_ENDPOINT is unset, so this is not required offline.
azure-ai-documentintelligence==1.0.2
reportlab

# Parses every .sql file as T-SQL. Catches typos and malformed statements only — it is
# a parser, not SQL Server, and would not have caught the bugs that reached Azure.
# See tests/test_sql_parses.py.
sqlfluff==4.3.0
